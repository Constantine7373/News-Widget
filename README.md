Установка сборщика
npm i


Запуск сборщика
gulp

Мок с json новостями лежит в 'src/resources/js/db.json'

Виджет подключаеся на  страницу 

let widget = new NewsWidget(selector), где selector = заранее созданный контейнер под него
